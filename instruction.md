运维人员把仪表盘、文件夹和连接权限从一个 Grafana 实例恢复到另一个实例时，用户、团队、文件夹和资源的数字 ID 往往不同，而当前实现有的整组覆盖、有的先清除再逐条添加，缺失主体或中途 API 失败会让目标停留在权限缺失或只恢复一部分的状态。请为三类权限建立统一的协调流程：先按登录名、团队名、嵌套文件夹路径和资源稳定标识解析整批备份，生成可供人类或 JSON 消费的确定性变更计划并支持不写入的预览；验证通过后只应用必要的新增、变更和删除，每个资源在任何步骤失败时都应尽力恢复开始前的可管理权限，并在补偿不完整时精确报告残留差异。继承、非可管理、内置角色和管理员保护项始终不得改动，旧版仪表盘/文件夹 ACL、Grafana v13 RBAC 与连接权限应具有一致的主体语义、过滤范围和幂等结果。
